# 校园专业心理医生工作流 Agent 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个面向青少年与大学生校园群体的专业心理医生工作流 Agent，支持日常倾诉、长期用户画像、焦虑/抑郁等常见心理问题风险筛查、危机识别、循证心理干预建议和随访预防。

**Architecture:** 项目采用 Python 后端优先架构，以 FastAPI 提供聊天、量表、画像、报告和管理 API；以 Agent Orchestrator 串联 LLM Provider、长期记忆、RAG 知识库、风险评估、干预推荐和安全策略。系统不宣称独立临床确诊，而是输出“疑似风险、筛查结果、证据链、干预建议、转介建议”，尽量贴近心理医生的评估与干预流程。

**Tech Stack:** Python 3.11+, FastAPI, Pydantic v2, SQLAlchemy 2.0 async, Alembic, PostgreSQL + pgvector, Redis, Celery 或 Arq, LangGraph, httpx, structlog, pytest, ruff, mypy, Docker Compose。

---

## 1. 产品边界与核心目标

### 1.1 项目定位

项目不是普通聊天机器人，而是“心理医生工作流增强型 Agent”。用户可以像日常聊天一样倾诉，系统在后台持续完成：

- 治疗性对话：共情、澄清、情绪命名、开放式提问。
- 结构化评估：症状、持续时间、频率、功能受损、诱因、保护因素。
- 用户画像：情绪趋势、睡眠、学业压力、人际关系、家庭压力、自我评价、回避行为。
- 疑似问题判断：焦虑风险、抑郁风险、失眠风险、自伤/自杀危机风险。
- 循证干预：心理教育、CBT 认知重构、行为激活、正念练习、睡眠卫生、求助转介。
- 长期随访：复测提醒、风险趋势变化、干预任务完成情况、复发预警。

### 1.2 安全与合规边界

系统输出应使用以下表达：

- “存在中度焦虑风险”
- “建议完成 GAD-7 进一步筛查”
- “从你的描述看，近期有明显抑郁相关信号”
- “建议联系学校心理中心或专业医生进一步评估”

系统不应输出以下表达：

- “你已经患有抑郁症”
- “你不需要看医生”
- “按我说的停药/吃药”
- “我可以替代心理医生”

危机风险场景下，系统必须优先执行安全响应：确认用户当前安全、建议联系可信成年人/学校心理中心/当地紧急热线、必要时建议立即拨打急救电话。

---

## 2. 推荐项目目录

```text
campus-psy-agent/
  app/
    main.py
    api/
      deps.py
      routes/
        chat.py
        assessment.py
        profile.py
        report.py
        health.py
    core/
      config.py
      logging.py
      security.py
      errors.py
      middleware.py
    db/
      session.py
      base.py
      models.py
      repositories/
        conversation_repo.py
        profile_repo.py
        assessment_repo.py
        audit_repo.py
    schemas/
      chat.py
      assessment.py
      profile.py
      risk.py
      report.py
    llm/
      base.py
      openai_provider.py
      qwen_provider.py
      deepseek_provider.py
      local_provider.py
      prompt_registry.py
    agent/
      orchestrator.py
      state.py
      nodes/
        safety_node.py
        memory_node.py
        intent_node.py
        assessment_node.py
        rag_node.py
        intervention_node.py
        response_node.py
    clinical/
      signal_extractor.py
      case_formulation.py
      risk_engine.py
      scales/
        phq9.py
        gad7.py
        cssrs_like.py
      interventions/
        cbt.py
        behavioral_activation.py
        mindfulness.py
        sleep_hygiene.py
        referral.py
      policies/
        safety_policy.py
        response_policy.py
    rag/
      chunker.py
      embeddings.py
      vector_store.py
      retriever.py
      knowledge_loader.py
    memory/
      conversation_memory.py
      profile_memory.py
      summarizer.py
    observability/
      events.py
      audit.py
      metrics.py
    workers/
      tasks.py
  knowledge_base/
    depression.md
    anxiety.md
    cbt_basics.md
    mindfulness.md
    sleep.md
    campus_resources.md
  tests/
    unit/
    integration/
    fixtures/
  alembic/
  docker-compose.yml
  pyproject.toml
  README.md
  .env.example
```

---

## 3. 系统总流程

一次用户聊天请求的完整链路：

1. `POST /api/chat/messages` 接收用户消息。
2. `RequestIdMiddleware` 注入 `request_id`，记录请求开始日志。
3. `SafetyNode` 先做危机词、强烈自伤/自杀表达、暴力风险的快速筛查。
4. `MemoryNode` 读取最近对话、长期画像、最近量表结果和风险趋势。
5. `IntentNode` 判断当前意图：倾诉、求建议、量表、危机、复盘、闲聊。
6. `SignalExtractor` 从对话中抽取症状、情绪、压力源、持续时间、功能受损和保护因素。
7. `AssessmentNode` 根据对话信号判断是否触发 PHQ-9、GAD-7 或危机追问。
8. `RiskEngine` 生成多轴风险结果：抑郁、焦虑、失眠、危机、功能受损。
9. `RagNode` 检索专业知识库：CBT、行为激活、正念、睡眠卫生、校园求助资源。
10. `InterventionNode` 选择干预策略：当下稳定、认知重构、行动计划、转介建议。
11. `ResponseNode` 生成用户可读回复，语气共情、非诊断、可执行。
12. 保存对话、画像更新、评估结果、干预建议、审计证据链。
13. 返回 `assistant_message`、`risk_summary`、`suggested_actions`、`follow_up_questions`。

---

## 4. 数据模型设计

### 4.1 users

- `id`: UUID
- `nickname`: str
- `age_group`: enum, `middle_school | high_school | undergraduate | graduate | unknown`
- `school_stage`: str
- `created_at`: datetime
- `updated_at`: datetime

### 4.2 conversations

- `id`: UUID
- `user_id`: UUID
- `title`: str
- `created_at`: datetime
- `updated_at`: datetime

### 4.3 messages

- `id`: UUID
- `conversation_id`: UUID
- `role`: enum, `user | assistant | system`
- `content`: text
- `content_hash`: str
- `risk_snapshot`: jsonb
- `created_at`: datetime

### 4.4 user_profiles

- `id`: UUID
- `user_id`: UUID
- `profile_json`: jsonb
- `latest_summary`: text
- `risk_trend_json`: jsonb
- `updated_by_message_id`: UUID
- `created_at`: datetime
- `updated_at`: datetime

`profile_json` 示例：

```json
{
  "dominant_emotions": ["焦虑", "低落", "疲惫"],
  "stressors": ["考试压力", "宿舍关系", "家庭期待"],
  "sleep": {"status": "变差", "evidence": ["最近总是凌晨两三点才睡"]},
  "social": {"status": "退缩", "evidence": ["不太想见同学"]},
  "function_impairment": ["学习效率下降", "注意力下降"],
  "protective_factors": ["愿意求助", "有一个可信朋友"],
  "risk_factors": ["持续失眠", "强烈自责"]
}
```

### 4.5 assessments

- `id`: UUID
- `user_id`: UUID
- `conversation_id`: UUID
- `scale_type`: enum, `phq9 | gad7 | cssrs_like`
- `answers`: jsonb
- `score`: int
- `severity`: str
- `interpretation`: text
- `created_at`: datetime

### 4.6 risk_assessments

- `id`: UUID
- `user_id`: UUID
- `conversation_id`: UUID
- `message_id`: UUID
- `depression_risk`: enum, `none | mild | moderate | moderately_severe | severe | unknown`
- `anxiety_risk`: enum, `none | mild | moderate | severe | unknown`
- `sleep_risk`: enum, `none | mild | moderate | severe | unknown`
- `crisis_level`: enum, `s0 | s1 | s2 | s3 | s4`
- `function_impairment_level`: enum, `none | mild | moderate | severe | unknown`
- `evidence`: jsonb
- `recommended_next_step`: jsonb
- `created_at`: datetime

### 4.7 audit_logs

- `id`: UUID
- `request_id`: str
- `user_id`: UUID
- `conversation_id`: UUID
- `event_type`: str
- `event_payload`: jsonb
- `created_at`: datetime

---

## 5. 日志与可观测性设计

整个项目每个关键部分都必须记录结构化日志，统一使用 `structlog` 输出 JSON 日志。

### 5.1 日志原则

- 每个请求必须有 `request_id`。
- 每轮对话必须有 `conversation_id`。
- 每次 Agent 执行必须有 `agent_run_id`。
- 不在普通日志中记录完整原文，默认记录 `content_hash`、长度、风险摘要和脱敏片段。
- 危机风险、安全策略、转介建议必须写入 `audit_logs`。
- LLM 调用必须记录 provider、model、latency、token_usage、status、error_type。
- RAG 检索必须记录 query_hash、top_k、chunk_ids、score。
- 风险判断必须记录 evidence_id、规则触发项、量表分数、模型判断版本。

### 5.2 通用日志字段

```json
{
  "timestamp": "2026-06-24T10:00:00Z",
  "level": "info",
  "event": "risk.assessment.completed",
  "request_id": "req_...",
  "agent_run_id": "run_...",
  "user_id_hash": "u_...",
  "conversation_id": "conv_...",
  "module": "clinical.risk_engine",
  "latency_ms": 138,
  "status": "success"
}
```

### 5.3 各模块必须记录的日志

| 模块 | 开始日志 | 成功日志 | 失败日志 | 审计日志 |
|---|---|---|---|---|
| API | `api.request.started` | `api.request.completed` | `api.request.failed` | 高风险请求 |
| Agent 编排 | `agent.run.started` | `agent.run.completed` | `agent.run.failed` | 完整节点路径 |
| SafetyNode | `safety.check.started` | `safety.check.completed` | `safety.check.failed` | S2-S4 必写 |
| MemoryNode | `memory.retrieve.started` | `memory.retrieve.completed` | `memory.retrieve.failed` | 不需要 |
| SignalExtractor | `signals.extract.started` | `signals.extract.completed` | `signals.extract.failed` | 中高风险证据 |
| RiskEngine | `risk.assessment.started` | `risk.assessment.completed` | `risk.assessment.failed` | 所有风险结果 |
| ScaleEngine | `scale.scoring.started` | `scale.scoring.completed` | `scale.scoring.failed` | 量表结果 |
| RAG | `rag.retrieve.started` | `rag.retrieve.completed` | `rag.retrieve.failed` | 高风险引用 |
| LLM Provider | `llm.call.started` | `llm.call.completed` | `llm.call.failed` | 高风险回复 |
| Intervention | `intervention.plan.started` | `intervention.plan.completed` | `intervention.plan.failed` | 转介建议 |
| DB | `db.query.started` | `db.query.completed` | `db.query.failed` | 写操作摘要 |

### 5.4 日志文件建议

- 开发环境：控制台 JSON 日志 + `logs/app.log`
- 测试环境：`logs/test.log`
- 生产模拟环境：按天切分 `logs/app-YYYY-MM-DD.log`
- 审计事件：数据库 `audit_logs` 表，不只写文件

---

## 6. Agent 核心实现方案

### 6.1 Agent 状态

`app/agent/state.py` 定义：

```python
from typing import Any, Literal
from pydantic import BaseModel, Field


class AgentState(BaseModel):
    request_id: str
    agent_run_id: str
    user_id: str
    conversation_id: str
    user_message: str
    intent: str | None = None
    recent_messages: list[dict[str, Any]] = Field(default_factory=list)
    profile: dict[str, Any] = Field(default_factory=dict)
    extracted_signals: dict[str, Any] = Field(default_factory=dict)
    risk_result: dict[str, Any] = Field(default_factory=dict)
    retrieved_knowledge: list[dict[str, Any]] = Field(default_factory=list)
    intervention_plan: dict[str, Any] = Field(default_factory=dict)
    response_text: str | None = None
    route: Literal["normal", "assessment", "crisis"] = "normal"
```

### 6.2 节点顺序

推荐使用 LangGraph：

```text
start
  -> safety_node
  -> memory_node
  -> intent_node
  -> signal_extraction_node
  -> assessment_node
  -> risk_node
  -> rag_node
  -> intervention_node
  -> response_node
  -> persist_node
end
```

如果 `safety_node` 判断为 S3 或 S4，直接进入：

```text
safety_node -> crisis_response_node -> persist_node -> end
```

---

## 7. 专业心理评估实现方案

### 7.1 对话信号抽取

`SignalExtractor` 抽取以下字段：

- `emotions`: 焦虑、低落、愤怒、羞耻、孤独、麻木。
- `symptoms`: 失眠、食欲变化、疲惫、注意力下降、自责、兴趣下降、坐立不安。
- `duration`: 持续时间，如“一周”“两周以上”“几个月”。
- `frequency`: 每天、每周几次、偶尔。
- `stressors`: 学业、考试、升学、就业、人际、家庭、亲密关系。
- `function_impairment`: 学习、社交、睡眠、生活自理。
- `risk_markers`: 自伤、自杀、绝望、无价值感、计划、工具、近期尝试。
- `protective_factors`: 求助意愿、朋友支持、家庭支持、未来计划。

### 7.2 PHQ-9

用于抑郁风险筛查，输出：

- `score`
- `severity`
- `item_9_positive`
- `recommended_next_step`

建议分层：

- 0-4：无明显或最低风险
- 5-9：轻度
- 10-14：中度
- 15-19：中重度
- 20-27：重度

如果第 9 题大于 0，必须触发危机风险复核。

### 7.3 GAD-7

用于焦虑风险筛查，输出：

- `score`
- `severity`
- `recommended_next_step`

建议分层：

- 0-4：无明显或最低风险
- 5-9：轻度
- 10-14：中度
- 15-21：重度

### 7.4 C-SSRS-like 危机筛查

为了项目实现，可以做简化版危机筛查，不直接复制量表全文，而是实现以下判断维度：

- 是否有“希望消失/睡着不醒/不想活”的被动想法。
- 是否有主动自杀想法。
- 是否想过具体方式。
- 是否有具体计划。
- 是否有实施意图。
- 是否已经准备工具或有近期尝试。

输出：

- `crisis_level`: `s0 | s1 | s2 | s3 | s4`
- `immediacy`: `none | recent | current`
- `protective_factors`
- `safety_response_required`

---

## 8. 干预策略推荐方案

### 8.1 干预类型

| 问题类型 | 轻度 | 中度 | 重度或危机 |
|---|---|---|---|
| 焦虑 | 心理教育、呼吸练习、担忧记录 | CBT 自动化想法、暴露小任务、正念 | 建议线下咨询，若伴随危机进入安全流程 |
| 抑郁 | 行为激活、作息恢复、情绪记录 | CBT 认知重构、活动计划、支持系统 | 建议专业评估，危机时安全计划 |
| 失眠 | 睡眠卫生、刺激控制 | 作息计划、睡前担忧卸载 | 长期严重失眠建议就医 |
| 学业压力 | 任务拆分、时间盒、失败归因调整 | 认知重构、问题解决疗法 | 若伴随严重功能受损建议咨询 |
| 人际关系 | 情绪表达、沟通脚本 | 边界训练、冲突复盘 | 涉及霸凌或暴力时建议求助 |

### 8.2 回复结构

普通场景回复：

```text
1. 先共情和复述用户处境。
2. 给出非诊断性理解。
3. 指出观察到的风险信号。
4. 给出一个当下可做的稳定练习。
5. 给出一个 24 小时内可完成的小行动。
6. 提出一个跟进问题。
```

危机场景回复：

```text
1. 明确表达关心和重视。
2. 询问用户当前是否安全、是否身边有人。
3. 建议立即联系可信成年人、学校心理中心、当地危机热线或急救电话。
4. 避免长篇分析和复杂任务。
5. 不承诺保密到危及生命的程度。
```

---

## 9. RAG 知识库实现方案

### 9.1 知识库内容

首期只放可控、可审查、简短的专业内容：

- 抑郁症状与筛查说明
- 焦虑症状与筛查说明
- CBT 基础：自动化想法、认知偏差、认知重构
- 行为激活：活动-情绪关系、低成本行动计划
- 正念练习：呼吸、身体扫描、 grounding
- 睡眠卫生：作息、光照、咖啡因、睡前屏幕
- 校园求助：心理中心、辅导员、可信同伴、家长
- 危机应对：安全计划、紧急联系、危机热线

### 9.2 检索策略

- 普通对话：检索 top 3。
- 中度风险：检索 top 5，必须包含干预知识。
- 危机风险：不走普通 RAG，直接走安全策略库。
- 回复时不暴露过多专业术语，后台保留知识引用。

---

## 10. API 设计

### 10.1 聊天接口

`POST /api/chat/messages`

请求：

```json
{
  "user_id": "uuid",
  "conversation_id": "uuid",
  "message": "我最近压力很大，晚上总是睡不着"
}
```

响应：

```json
{
  "message_id": "uuid",
  "assistant_message": "听起来你最近一直处在很紧绷的状态...",
  "risk_summary": {
    "depression_risk": "unknown",
    "anxiety_risk": "mild",
    "crisis_level": "s0"
  },
  "suggested_actions": [
    "完成一次 GAD-7 筛查",
    "今晚尝试 3 分钟呼吸练习"
  ],
  "follow_up_questions": [
    "这种睡不着大概持续多久了？"
  ]
}
```

### 10.2 量表接口

- `POST /api/assessments/phq9`
- `POST /api/assessments/gad7`
- `POST /api/assessments/crisis`

### 10.3 用户画像接口

- `GET /api/profile/{user_id}`
- `GET /api/profile/{user_id}/timeline`

### 10.4 报告接口

- `GET /api/report/{user_id}/latest`
- `POST /api/report/{user_id}/generate`

报告包括：

- 近期情绪变化
- 主要压力源
- 疑似风险
- 功能受损
- 保护因素
- 推荐干预
- 是否建议线下求助

---

## 11. 开发待办清单

### Task 1: 初始化项目骨架

**Files:**
- Create: `pyproject.toml`
- Create: `.env.example`
- Create: `app/main.py`
- Create: `app/core/config.py`
- Create: `app/core/logging.py`
- Create: `app/core/middleware.py`
- Create: `tests/unit/test_health.py`

- [ ] 创建 Python 3.11 FastAPI 项目，配置 `ruff`、`mypy`、`pytest`。
- [ ] 实现 `/api/health`，返回 `{"status": "ok"}`。
- [ ] 实现 `RequestIdMiddleware`，为每个请求注入 `request_id`。
- [ ] 实现 `structlog` JSON 日志，启动时输出 `app.startup`。
- [ ] 编写健康检查测试。
- [ ] 运行 `pytest tests/unit/test_health.py -v`，预期通过。

### Task 2: 数据库与基础模型

**Files:**
- Create: `app/db/session.py`
- Create: `app/db/base.py`
- Create: `app/db/models.py`
- Create: `alembic/env.py`
- Create: `tests/unit/test_models.py`

- [ ] 配置 SQLAlchemy async engine。
- [ ] 定义 `User`、`Conversation`、`Message`、`UserProfile`、`Assessment`、`RiskAssessment`、`AuditLog`。
- [ ] 为 JSON 字段使用 PostgreSQL `JSONB`。
- [ ] 为消息表保存 `content_hash`，避免日志直接依赖原文。
- [ ] 编写模型创建测试。
- [ ] 运行数据库相关单测。

### Task 3: 审计日志与事件记录

**Files:**
- Create: `app/observability/events.py`
- Create: `app/observability/audit.py`
- Create: `app/db/repositories/audit_repo.py`
- Create: `tests/unit/test_audit.py`

- [ ] 定义统一事件类型：`api.request.started`、`agent.run.started`、`risk.assessment.completed`、`safety.escalation.triggered`。
- [ ] 实现 `AuditLogger.record_event()`。
- [ ] 对 S2-S4 危机事件强制写入 `audit_logs`。
- [ ] 编写测试：危机事件必须落库，普通事件可只输出结构化日志。

### Task 4: LLM Provider 可插拔适配层

**Files:**
- Create: `app/llm/base.py`
- Create: `app/llm/openai_provider.py`
- Create: `app/llm/qwen_provider.py`
- Create: `app/llm/deepseek_provider.py`
- Create: `app/llm/local_provider.py`
- Create: `app/llm/prompt_registry.py`
- Create: `tests/unit/test_llm_provider.py`

- [ ] 定义 `LLMProvider` 抽象类，包含 `chat()` 和 `embed()`。
- [ ] 实现 API 模型 Provider，开发阶段优先使用环境变量选择。
- [ ] 实现 `LocalProvider` 空壳适配，预留本地模型部署。
- [ ] 每次 LLM 调用记录 `llm.call.started`、`llm.call.completed`、`llm.call.failed`。
- [ ] 日志记录模型名、延迟、token 用量、错误类型，不记录完整 prompt。

### Task 5: 量表评分模块

**Files:**
- Create: `app/clinical/scales/phq9.py`
- Create: `app/clinical/scales/gad7.py`
- Create: `app/clinical/scales/cssrs_like.py`
- Create: `tests/unit/test_scales.py`

- [ ] 实现 PHQ-9 评分与严重程度映射。
- [ ] 实现 GAD-7 评分与严重程度映射。
- [ ] 实现简化危机筛查评分，输出 S0-S4。
- [ ] PHQ-9 第 9 题大于 0 时，强制标记需要危机复核。
- [ ] 每次量表评分记录 `scale.scoring.completed`。

### Task 6: 对话信号抽取

**Files:**
- Create: `app/clinical/signal_extractor.py`
- Create: `app/schemas/risk.py`
- Create: `tests/unit/test_signal_extractor.py`

- [ ] 定义结构化输出 schema：情绪、症状、持续时间、频率、压力源、功能受损、危机信号、保护因素。
- [ ] 先实现规则抽取：关键词、时间表达、自伤表达。
- [ ] 再接入 LLM 结构化抽取，使用 Pydantic 校验输出。
- [ ] 抽取完成记录 `signals.extract.completed`，包含字段数量和风险标记。

### Task 7: 风险评估引擎

**Files:**
- Create: `app/clinical/risk_engine.py`
- Create: `tests/unit/test_risk_engine.py`

- [ ] 融合对话信号、PHQ-9、GAD-7、危机筛查结果。
- [ ] 输出 `depression_risk`、`anxiety_risk`、`sleep_risk`、`crisis_level`、`function_impairment_level`。
- [ ] 输出 `evidence`，每个风险判断必须包含证据来源。
- [ ] S3-S4 直接触发安全流程。
- [ ] 风险评估完成记录 `risk.assessment.completed`。

### Task 8: 用户画像与长期记忆

**Files:**
- Create: `app/memory/conversation_memory.py`
- Create: `app/memory/profile_memory.py`
- Create: `app/memory/summarizer.py`
- Create: `app/db/repositories/profile_repo.py`
- Create: `tests/unit/test_profile_memory.py`

- [ ] 保存最近 N 轮对话。
- [ ] 根据新信号增量更新 `user_profiles.profile_json`。
- [ ] 对长期对话做摘要，记录情绪、压力源、功能受损和保护因素变化。
- [ ] 更新画像时记录 `profile.update.completed`。

### Task 9: RAG 知识库

**Files:**
- Create: `app/rag/chunker.py`
- Create: `app/rag/embeddings.py`
- Create: `app/rag/vector_store.py`
- Create: `app/rag/retriever.py`
- Create: `app/rag/knowledge_loader.py`
- Create: `knowledge_base/depression.md`
- Create: `knowledge_base/anxiety.md`
- Create: `knowledge_base/cbt_basics.md`
- Create: `knowledge_base/mindfulness.md`
- Create: `knowledge_base/sleep.md`
- Create: `knowledge_base/campus_resources.md`
- Create: `tests/unit/test_retriever.py`

- [ ] 编写首批知识库文档。
- [ ] 实现 Markdown 切片与 embedding。
- [ ] 写入 pgvector。
- [ ] 根据问题类型检索 top_k 知识。
- [ ] 检索完成记录 `rag.retrieve.completed`，包含 chunk_id 和 score。

### Task 10: 干预策略推荐器

**Files:**
- Create: `app/clinical/interventions/cbt.py`
- Create: `app/clinical/interventions/behavioral_activation.py`
- Create: `app/clinical/interventions/mindfulness.py`
- Create: `app/clinical/interventions/sleep_hygiene.py`
- Create: `app/clinical/interventions/referral.py`
- Create: `app/clinical/policies/safety_policy.py`
- Create: `tests/unit/test_interventions.py`

- [ ] 焦虑轻中度场景推荐呼吸练习、担忧记录、CBT 自动化想法记录。
- [ ] 抑郁轻中度场景推荐行为激活、活动计划、支持系统连接。
- [ ] 失眠场景推荐睡眠卫生和作息计划。
- [ ] S2-S4 场景禁止普通建议优先，进入安全策略。
- [ ] 干预推荐完成记录 `intervention.plan.completed`。

### Task 11: Agent 编排

**Files:**
- Create: `app/agent/state.py`
- Create: `app/agent/orchestrator.py`
- Create: `app/agent/nodes/safety_node.py`
- Create: `app/agent/nodes/memory_node.py`
- Create: `app/agent/nodes/intent_node.py`
- Create: `app/agent/nodes/assessment_node.py`
- Create: `app/agent/nodes/rag_node.py`
- Create: `app/agent/nodes/intervention_node.py`
- Create: `app/agent/nodes/response_node.py`
- Create: `tests/integration/test_agent_flow.py`

- [ ] 定义 `AgentState`。
- [ ] 使用 LangGraph 串联节点。
- [ ] 实现普通对话路径。
- [ ] 实现量表触发路径。
- [ ] 实现危机响应路径。
- [ ] 每个节点开始与结束都记录结构化日志。
- [ ] 集成测试覆盖普通倾诉、焦虑筛查、抑郁筛查、危机表达。

### Task 12: Chat API

**Files:**
- Create: `app/api/routes/chat.py`
- Create: `app/schemas/chat.py`
- Modify: `app/main.py`
- Create: `tests/integration/test_chat_api.py`

- [ ] 实现 `POST /api/chat/messages`。
- [ ] 请求进入时记录 `api.request.started`。
- [ ] 调用 Agent Orchestrator。
- [ ] 保存用户消息、助手回复、风险快照。
- [ ] 返回用户可读回复、风险摘要、建议行动和追问。
- [ ] API 完成时记录 `api.request.completed`。

### Task 13: 量表 API

**Files:**
- Create: `app/api/routes/assessment.py`
- Create: `app/schemas/assessment.py`
- Create: `tests/integration/test_assessment_api.py`

- [ ] 实现 PHQ-9 提交接口。
- [ ] 实现 GAD-7 提交接口。
- [ ] 实现危机筛查提交接口。
- [ ] 量表提交后写入 `assessments`。
- [ ] 同步刷新 `risk_assessments`。
- [ ] 量表 API 全链路记录日志。

### Task 14: 画像与报告 API

**Files:**
- Create: `app/api/routes/profile.py`
- Create: `app/api/routes/report.py`
- Create: `app/schemas/profile.py`
- Create: `app/schemas/report.py`
- Create: `tests/integration/test_profile_report_api.py`

- [ ] 实现 `GET /api/profile/{user_id}`。
- [ ] 实现 `GET /api/profile/{user_id}/timeline`。
- [ ] 实现 `GET /api/report/{user_id}/latest`。
- [ ] 报告包含心理画像、风险趋势、证据摘要、建议干预、是否建议线下求助。
- [ ] 报告生成记录 `report.generate.completed`。

### Task 15: 评估集与效果验证

**Files:**
- Create: `tests/fixtures/dialogues/depression_mild.json`
- Create: `tests/fixtures/dialogues/anxiety_moderate.json`
- Create: `tests/fixtures/dialogues/crisis_s3.json`
- Create: `tests/integration/test_evaluation_cases.py`
- Create: `app/observability/metrics.py`

- [ ] 构造 20 条校园场景测试对话。
- [ ] 覆盖普通压力、焦虑、抑郁、失眠、人际关系、自伤表达。
- [ ] 检查系统是否触发正确量表与风险分层。
- [ ] 检查危机样例是否进入安全流程。
- [ ] 输出评估指标：风险召回、危机召回、误触发率、回复安全性。

### Task 16: Docker 与运行文档

**Files:**
- Create: `docker-compose.yml`
- Create: `README.md`
- Create: `.env.example`

- [ ] 配置 FastAPI、PostgreSQL、Redis。
- [ ] 配置 pgvector 扩展。
- [ ] README 写清启动命令、环境变量、测试命令、API 示例。
- [ ] 提供一组演示对话，方便秋招面试展示。

---

## 12. 面试展示亮点

1. **不是普通聊天 Agent**：系统有多轴评估、画像、量表、RAG、干预策略和随访。
2. **工程化 Agent 编排**：LangGraph 节点化流程，支持普通、量表、危机三条路径。
3. **可插拔模型层**：API 模型优先，预留本地模型 Provider。
4. **专业知识增强**：CBT、行为激活、正念、睡眠卫生和校园求助知识库。
5. **长期用户画像**：从多轮对话沉淀情绪趋势、压力源、功能受损和保护因素。
6. **安全与审计**：高风险场景全链路记录证据、规则、模型判断和最终回复。
7. **可验证**：用固定对话评估风险召回、危机召回、误触发率和回复安全性。

---

## 13. 第一阶段 MVP 范围

第一阶段只做以下闭环：

- 日常聊天 API
- 用户画像更新
- PHQ-9 / GAD-7 / 简化危机筛查
- 焦虑、抑郁、危机风险判断
- CBT / 行为激活 / 正念 / 睡眠干预推荐
- RAG 专业知识库
- 审计日志与结构化日志
- 简单报告 API

暂不做：

- 药物建议
- 医生端后台
- 多模态语音/表情识别
- 真实学校系统集成
- 完整移动端 App
- 独立临床诊断

---

## 14. 建议开发顺序

1. 项目骨架、日志、中间件。
2. 数据库模型和审计日志。
3. 量表评分模块。
4. 对话信号抽取。
5. 风险评估引擎。
6. 用户画像与长期记忆。
7. RAG 知识库。
8. 干预策略推荐器。
9. Agent 编排。
10. Chat API。
11. 画像、量表、报告 API。
12. 评估集与演示样例。
13. Docker、README、简历描述。

---

## 15. 自检清单

- [ ] 所有 API 请求都有 `request_id`。
- [ ] 所有 Agent 执行都有 `agent_run_id`。
- [ ] 所有 LLM 调用都有耗时、模型名、状态日志。
- [ ] 所有风险判断都有证据链。
- [ ] 所有 S2-S4 危机事件写入 `audit_logs`。
- [ ] PHQ-9 第 9 题阳性会触发危机复核。
- [ ] 系统不会输出独立确诊语句。
- [ ] 系统不会输出药物处方建议。
- [ ] RAG 知识库来源可控。
- [ ] 测试覆盖普通倾诉、焦虑、抑郁、危机场景。

